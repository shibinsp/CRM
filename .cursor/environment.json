{
  "install": "yarn install",
  "start": "(sudo service docker start || service docker start || true) && bash packages/beeax-utils/setup-dev-env.sh && npx nx database:reset beeax-server",
  "terminals": [
    {
      "name": "Development Server",
      "command": "yarn start"
    }
  ]
}
