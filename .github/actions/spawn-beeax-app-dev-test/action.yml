name: Spawn BeeAX App Dev Test
description: >
  Starts a BeeAX all-in-one test instance (server, worker, database, redis)
  using the beeaxcrm/beeax-app-dev Docker image on port 2021.
  The server is available at http://localhost:2021 with seeded demo data.

inputs:
  beeax-version:
    description: 'BeeAX Docker Hub image tag for beeax-app-dev (e.g., "latest" or "v1.20.0").'
    required: false
    default: 'latest'

outputs:
  server-url:
    description: 'URL where the BeeAX test server can be reached'
    value: http://localhost:2021
  api-key:
    description: 'API key (type: API_KEY) for the seeded BeeAX dev workspace'
    value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..bfQjfyN0NEtTCLE_xPyNcwonDzlSXFoP8kdCQTdnuDc

runs:
  using: 'composite'
  steps:
    - name: Start beeax-app-dev-test container
      shell: bash
      run: |
        docker run -d \
          --name beeax-app-dev-test \
          -p 2021:2021 \
          -e NODE_PORT=2021 \
          -e SERVER_URL=http://localhost:2021 \
          -e MARKETPLACE_CATALOG_SYNC_CRON_ENABLED=false \
          beeaxcrm/beeax-app-dev:${{ inputs.beeax-version }}

        echo "Waiting for BeeAX test instance to become healthy…"
        TIMEOUT=180
        ELAPSED=0
        until curl -sf http://localhost:2021/healthz > /dev/null 2>&1; do
          if [ "$ELAPSED" -ge "$TIMEOUT" ]; then
            echo "::error::BeeAX did not become healthy within ${TIMEOUT}s"
            docker logs beeax-app-dev-test 2>&1 | tail -80
            exit 1
          fi
          sleep 3
          ELAPSED=$((ELAPSED + 3))
          echo "  … waited ${ELAPSED}s"
        done
        echo "BeeAX test instance is ready at http://localhost:2021 (took ~${ELAPSED}s)"
