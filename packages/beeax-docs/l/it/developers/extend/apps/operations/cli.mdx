---
title: CLI
description: Comandi di `yarn beeax` per eseguire funzioni, eseguire lo streaming dei log, gestire le installazioni delle app e passare da un remoto all'altro.
icon: terminale
---

Oltre a `dev`, `dev:build`, `dev:add` e `dev:typecheck`, la CLI `yarn beeax` fornisce comandi per eseguire funzioni, visualizzare i log e gestire le installazioni delle app.

## Esecuzione delle funzioni (`yarn beeax dev:function:exec`)

Esegui manualmente una funzione logica senza attivarla tramite HTTP, cron o evento del database:

```bash filename="Terminal"
# Execute by function name
yarn beeax dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn beeax dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn beeax dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the post-install function
yarn beeax dev:function:exec --postInstall
```

## Visualizzazione dei log delle funzioni (`yarn beeax dev:function:logs`)

Esegui lo streaming dei log di esecuzione per le funzioni logiche della tua app:

```bash filename="Terminal"
# Stream all function logs
yarn beeax dev:function:logs

# Filter by function name
yarn beeax dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn beeax dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
Questo è diverso da `yarn beeax docker:logs`, che mostra i log del container Docker. `yarn beeax dev:function:logs` mostra i log di esecuzione delle funzioni della tua app dal server BeeAX.
</Note>

## Generare il client tipizzato (`yarn beeax dev:generate-client`)

Rigenera il client API tipizzato (`beeax-client-sdk`) dallo schema del remoto attivo, senza eseguire la build né sincronizzare un'app. Usalo per ottenere un client tipizzato in qualsiasi progetto — ad esempio un servizio backend che vive in un repository separato — che comunica con la tua istanza BeeAX:

```bash filename="Terminal"
# In your project (no BeeAX app definition required)
yarn add beeax-sdk beeax-client-sdk

# Connect to the BeeAX instance to generate the client from
yarn beeax remote:add

# Generate the typed client into node_modules/beeax-client-sdk
yarn beeax dev:generate-client
```

Quindi importa il client nel tuo codice:

```typescript
import { CoreApiClient } from 'beeax-client-sdk/core';
```

Esegui nuovamente il comando ogni volta che il tuo modello di dati cambia, per aggiornare i tipi generati.

<Note>
Il client viene generato all'interno di `node_modules`, quindi non viene inserito nel commit insieme al tuo codice. Esegui `yarn beeax dev:generate-client` dopo ogni installazione (ad esempio in uno script `postinstall` o nella CI).
</Note>

## Disinstallazione di un'app (`yarn beeax app:uninstall`)

Rimuovi la tua app dallo spazio di lavoro attivo:

```bash filename="Terminal"
yarn beeax app:uninstall

# Skip the confirmation prompt
yarn beeax app:uninstall --yes
```

## Gestione dei remoti

Un **remoto** è un server BeeAX a cui la tua app si connette. Durante la configurazione, lo strumento di scaffolding ne crea uno automaticamente per te. Puoi aggiungere altri remoti o passare da uno all'altro in qualsiasi momento.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn beeax remote:add

# Connect to a local BeeAX server (auto-detects port 2020 or 3000)
yarn beeax remote:add --local

# Add a remote non-interactively (useful for CI)
yarn beeax remote:add --url https://your-beeax-server.com --api-key $TWENTY_API_KEY --as my-remote

# List all configured remotes
yarn beeax remote:list

# Set the active remote
yarn beeax remote:use <name>
```

Le tue credenziali sono archiviate in `~/.beeax/config.json`.
