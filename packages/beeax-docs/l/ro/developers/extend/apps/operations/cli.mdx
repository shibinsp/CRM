---
title: CLI
description: comenzi `yarn beeax` pentru executarea funcțiilor, transmiterea în flux a jurnalelor, gestionarea instalărilor de aplicații și comutarea între remote-uri.
icon: terminal
---

Dincolo de `dev`, `dev:build`, `dev:add` și `dev:typecheck`, `yarn beeax` CLI oferă comenzi pentru executarea funcțiilor, vizualizarea jurnalelor și gestionarea instalărilor de aplicații.

## Executarea funcțiilor (`yarn beeax dev:function:exec`)

Rulați manual o funcție logică fără a o declanșa prin HTTP, cron sau eveniment de bază de date:

```bash filename="Terminal"
# Execute by function name
yarn beeax dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn beeax dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn beeax dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the post-install function
yarn beeax dev:function:exec --postInstall
```

## Vizualizarea jurnalelor funcțiilor (`yarn beeax dev:function:logs`)

Transmiteți în flux jurnalele de execuție pentru funcțiile logice ale aplicației:

```bash filename="Terminal"
# Stream all function logs
yarn beeax dev:function:logs

# Filter by function name
yarn beeax dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn beeax dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
Acest lucru este diferit de `yarn beeax docker:logs`, care afișează jurnalele containerului Docker. `yarn beeax dev:function:logs` afișează jurnalele de execuție ale funcțiilor aplicației de pe serverul BeeAX.
</Note>

## Generarea clientului tipizat (`yarn beeax dev:generate-client`)

Regenerați clientul tipizat al API-ului (`beeax-client-sdk`) din schema remote-ului activ, fără a construi sau sincroniza o aplicație. Folosiți-l pentru a obține un client tipizat în orice proiect — cum ar fi un serviciu backend aflat într-un repository separat — care comunică cu instanța dvs. BeeAX:

```bash filename="Terminal"
# In your project (no BeeAX app definition required)
yarn add beeax-sdk beeax-client-sdk

# Connect to the BeeAX instance to generate the client from
yarn beeax remote:add

# Generate the typed client into node_modules/beeax-client-sdk
yarn beeax dev:generate-client
```

Apoi importați clientul în codul dvs.:

```typescript
import { CoreApiClient } from 'beeax-client-sdk/core';
```

Rulați din nou comanda ori de câte ori se schimbă modelul de date pentru a reîmprospăta tipurile generate.

<Note>
Clientul este generat în interiorul `node_modules`, astfel încât nu este inclus în commit împreună cu codul dvs. Rulați `yarn beeax dev:generate-client` după fiecare instalare (de exemplu, într-un script `postinstall` sau în CI).
</Note>

## Dezinstalarea unei aplicații (`yarn beeax app:uninstall`)

Eliminați aplicația din spațiul de lucru activ:

```bash filename="Terminal"
yarn beeax app:uninstall

# Skip the confirmation prompt
yarn beeax app:uninstall --yes
```

## Gestionarea remote-urilor

Un „remote” este un server BeeAX la care se conectează aplicația. În timpul configurării, Scaffolderul creează automat unul pentru dvs. Puteți adăuga mai multe remote-uri sau comuta între ele oricând.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn beeax remote:add

# Connect to a local BeeAX server (auto-detects port 2020 or 3000)
yarn beeax remote:add --local

# Add a remote non-interactively (useful for CI)
yarn beeax remote:add --url https://your-beeax-server.com --api-key $TWENTY_API_KEY --as my-remote

# List all configured remotes
yarn beeax remote:list

# Set the active remote
yarn beeax remote:use <name>
```

Acreditările dvs. sunt stocate în `~/.beeax/config.json`.
