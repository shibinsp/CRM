---
title: Локальный сервер
description: Управление локальным Docker-сервером BeeAX — запуск, остановка, обновление, параллельный тестовый экземпляр и ручная настройка SDK.
icon: server
---

## Управление локальным сервером

Используйте `yarn beeax docker:*` для управления локальным контейнером BeeAX:

| Команда                                | Что делает                                           |
| -------------------------------------- | ---------------------------------------------------- |
| `yarn beeax docker:start`             | Запустить сервер (при необходимости скачивает образ) |
| `yarn beeax docker:start 2.2.0`       | Запустить конкретную версию сервера                  |
| `yarn beeax docker:start --port 3030` | Запустить на пользовательском порту                  |
| `yarn beeax docker:stop`              | Остановить сервер (данные сохраняются)               |
| `yarn beeax docker:status`            | Показать URL, версию и учётные данные для входа      |
| `yarn beeax docker:logs`              | Потоковый вывод журналов сервера                     |
| `yarn beeax docker:reset`             | Стереть данные и начать заново                       |
| `yarn beeax docker:upgrade`           | Скачать последний образ `beeax-app-dev`             |
| `yarn beeax docker:upgrade 2.2.0`     | Обновить до конкретной версии                        |

Данные сохраняются между перезапусками в двух томах Docker (`beeax-app-dev-data` для PostgreSQL, `beeax-app-dev-storage` для файлов). Используйте `reset`, чтобы стереть всё.

## Фиксация версии сервера

Когда версия не передана, `docker:start` определяет версию из диапазона `engines.beeax` вашего приложения в `package.json` — того же диапазона, по которому сервер выполняет проверку при установке вашего приложения. Он запускает самый новый опубликованный образ `beeax-app-dev`, который удовлетворяет диапазону, и использует `latest`, если поле отсутствует или ни одна опубликованная версия не совпадает:

```json filename="package.json"
{
  "engines": {
    "beeax": ">=2.2.0"
  }
}
```

Передайте версию явно, чтобы переопределить диапазон для одного запуска: `yarn beeax docker:start 2.3.0`. Если контейнер уже существует с другой версией, `docker:start` обновляет его на месте (пересоздаёт контейнер с сохранением томов данных).

## Обновление образа сервера

`yarn beeax docker:upgrade` скачивает последний образ, сравнивает дайджесты и пересоздаёт контейнер только если действительно что-то изменилось. Тома сохраняются — заменяется только контейнер. Если был скачан новый образ и контейнер работал, при обновлении автоматически запускается новый контейнер; затем выполните `yarn beeax docker:start`, чтобы дождаться его готовности.

```bash filename="Terminal"
yarn beeax docker:upgrade            # Latest
yarn beeax docker:upgrade 2.2.0      # Specific version
```

Проверьте запущенную версию с помощью `yarn beeax docker:status` (эта команда показывает `APP_VERSION`, встроенную в контейнер).

## Запуск параллельного тестового экземпляра

Передайте `--test` любой команде `docker:*`, чтобы управлять вторым, полностью изолированным экземпляром — это полезно для запуска интеграционных тестов или экспериментов, не затрагивая ваши основные данные разработки:

| Команда                             | Что делает                                              |
| ----------------------------------- | ------------------------------------------------------- |
| `yarn beeax docker:start --test`   | Запустить тестовый экземпляр (по умолчанию — порт 2021) |
| `yarn beeax docker:stop --test`    | Остановить его                                          |
| `yarn beeax docker:status --test`  | Показать его статус                                     |
| `yarn beeax docker:logs --test`    | Транслировать его журналы                               |
| `yarn beeax docker:reset --test`   | Стереть его данные                                      |
| `yarn beeax docker:upgrade --test` | Обновить его образ                                      |

Тестовый экземпляр запускается в собственном контейнере Docker (`beeax-app-dev-test`) с выделенными томами (`beeax-app-dev-test-data`, `beeax-app-dev-test-storage`) и собственной конфигурацией, поэтому он может работать параллельно с вашим основным экземпляром без конфликтов. Совместите `--test` с `--port`, чтобы переопределить значение по умолчанию (2021).

## Ручная настройка (без генератора каркаса)

Пропустите генератор каркаса, если вы добавляете SDK в существующий проект:

```bash filename="Terminal"
yarn add beeax-sdk beeax-client-sdk
```

Добавьте скрипт в `package.json`:

```json filename="package.json"
{
  "scripts": {
    "beeax": "beeax"
  }
}
```

Теперь вы можете запускать `yarn beeax dev`, `yarn beeax docker:start` и остальные команды.

<Note>
Не устанавливайте `beeax-sdk` глобально — фиксируйте версию в каждом проекте, чтобы каждое приложение использовало свою собственную версию.
</Note>
