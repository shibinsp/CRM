---
title: Serveur local
description: Gérez le serveur Docker local de BeeAX — démarrage, arrêt, mise à niveau, instance de test parallèle et configuration manuelle du SDK.
icon: server
---

## Gestion du serveur local

Utilisez `yarn beeax docker:*` pour contrôler le conteneur BeeAX local :

| Commande                               | Ce que cela fait                                           |
| -------------------------------------- | ---------------------------------------------------------- |
| `yarn beeax docker:start`             | Démarre le serveur (récupère l’image si nécessaire)        |
| `yarn beeax docker:start 2.2.0`       | Démarrer une version spécifique du serveur                 |
| `yarn beeax docker:start --port 3030` | Démarre sur un port personnalisé                           |
| `yarn beeax docker:stop`              | Arrête le serveur (préserve les données)                   |
| `yarn beeax docker:status`            | Affiche l’URL, la version et les identifiants de connexion |
| `yarn beeax docker:logs`              | Diffuse les journaux du serveur                            |
| `yarn beeax docker:reset`             | Efface les données et repart de zéro                       |
| `yarn beeax docker:upgrade`           | Récupère la dernière image `beeax-app-dev`                |
| `yarn beeax docker:upgrade 2.2.0`     | Met à niveau vers une version spécifique                   |

Les données sont conservées entre les redémarrages dans deux volumes Docker (`beeax-app-dev-data` pour PostgreSQL, `beeax-app-dev-storage` pour les fichiers). Utilisez `reset` pour tout effacer.

## Figer la version du serveur

Lorsqu’aucune version n’est fournie, `docker:start` résout la version à partir de la plage `engines.beeax` de votre `package.json` — la même plage que le serveur utilise pour la validation lors de l’installation de votre application. Il démarre l’image `beeax-app-dev` la plus récente publiée qui satisfait la plage, et revient à `latest` lorsque le champ est absent ou qu’aucune version publiée ne correspond :

```json filename="package.json"
{
  "engines": {
    "beeax": ">=2.2.0"
  }
}
```

Passez explicitement une version pour remplacer la plage pour une seule exécution : `yarn beeax docker:start 2.3.0`. Si un conteneur existe déjà avec une version différente, `docker:start` le met à niveau sur place (en recréant le conteneur tout en préservant vos volumes de données).

## Mise à niveau de l’image du serveur

`yarn beeax docker:upgrade` récupère la dernière image, compare les empreintes et ne recrée le conteneur que si quelque chose a réellement changé. Les volumes sont conservés — seul le conteneur est remplacé. Si une nouvelle image a été récupérée et que le conteneur était en cours d’exécution, la mise à niveau démarre automatiquement un nouveau conteneur ; exécutez ensuite `yarn beeax docker:start` pour attendre qu’il devienne opérationnel.

```bash filename="Terminal"
yarn beeax docker:upgrade            # Latest
yarn beeax docker:upgrade 2.2.0      # Specific version
```

Vérifiez la version en cours d’exécution avec `yarn beeax docker:status` (elle affiche la valeur de `APP_VERSION` intégrée au conteneur).

## Exécuter une instance de test parallèle

Passez `--test` à n’importe quelle commande `docker:*` pour gérer une deuxième instance totalement isolée — utile pour les tests d’intégration ou pour expérimenter sans toucher à vos données de développement principales :

| Commande                            | Ce que cela fait                                  |
| ----------------------------------- | ------------------------------------------------- |
| `yarn beeax docker:start --test`   | Démarre l’instance de test (port 2021 par défaut) |
| `yarn beeax docker:stop --test`    | L’arrête                                          |
| `yarn beeax docker:status --test`  | Affiche son statut                                |
| `yarn beeax docker:logs --test`    | Diffuse ses journaux                              |
| `yarn beeax docker:reset --test`   | Efface ses données                                |
| `yarn beeax docker:upgrade --test` | Met à niveau son image                            |

L’instance de test possède son propre conteneur (`beeax-app-dev-test`), ses volumes (`beeax-app-dev-test-data`, `beeax-app-dev-test-storage`) et sa configuration — elle s’exécute parallèlement à votre instance principale sans conflits. Combinez `--test` avec `--port` pour remplacer le port 2021.

## Configuration manuelle (sans le générateur)

Ignorez le générateur si vous ajoutez le SDK à un projet existant :

```bash filename="Terminal"
yarn add beeax-sdk beeax-client-sdk
```

Ajoutez le script à `package.json` :

```json filename="package.json"
{
  "scripts": {
    "beeax": "beeax"
  }
}
```

Vous pouvez maintenant exécuter `yarn beeax dev`, `yarn beeax docker:start` et le reste.

<Note>
N’installez pas `beeax-sdk` globalement — figez-le par projet afin que chaque application utilise sa propre version.
</Note>
