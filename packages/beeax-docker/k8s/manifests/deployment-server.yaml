apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: beeaxcrm-server
  name: beeaxcrm-server
  namespace: beeaxcrm
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: beeaxcrm-server
  template:
    metadata:
      labels:
        app: beeaxcrm-server
    spec:
      volumes:
        - name: beeaxcrm-server-data
          persistentVolumeClaim:
            claimName: beeaxcrm-server-pvc
        - name: beeaxcrm-docker-data
          persistentVolumeClaim:
            claimName: beeaxcrm-docker-data-pvc
      containers:
        - name: beeaxcrm
          image: beeaxcrm/beeax:latest
          imagePullPolicy: Always
          env:
            - name: NODE_PORT
              value: 3000
            - name: SERVER_URL
              value: "https://crm.example.com:443"
            - name: "PG_DATABASE_URL"
              value: "postgres://postgres:postgres@beeaxcrm-db.beeaxcrm.svc.cluster.local/default"
            - name: "REDIS_URL"
              value: "redis://beeaxcrm-redis.beeaxcrm.svc.cluster.local:6379"
            - name: SIGN_IN_PREFILLED
              value: "false"
            - name: STORAGE_TYPE
              value: "local"
            - name: "ACCESS_TOKEN_EXPIRES_IN"
              value: "7d"
            - name: "LOGIN_TOKEN_EXPIRES_IN"
              value: "1h"
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: tokens
                  key: accessToken
          ports:
            - containerPort: 3000
              name: http-tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /app/docker-data
              name: beeaxcrm-docker-data
            - mountPath: /app/packages/beeax-server/.local-storage
              name: beeaxcrm-server-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
