apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: beeaxcrm-db
  name: beeaxcrm-db
  namespace: beeaxcrm
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: beeaxcrm-db
  template:
    metadata:
      labels:
        app: beeaxcrm-db
    spec:
      volumes:
        - name: beeaxcrm-db-data
          persistentVolumeClaim:
            claimName: beeaxcrm-db-pvc
      containers:
        - name: beeaxcrm
          image: beeaxcrm/beeax-postgres-spilo:latest
          imagePullPolicy: Always
          env:
            - name: PGUSER_SUPERUSER
              value: "postgres"
            - name: PGPASSWORD_SUPERUSER
              value: "postgres"
            - name: SPILO_PROVIDER
              value: "local"
            - name: ALLOW_NOSSL
              value: "true"
          ports:
            - containerPort: 5432
              name: tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /home/postgres/pgdata
              name: beeaxcrm-db-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
