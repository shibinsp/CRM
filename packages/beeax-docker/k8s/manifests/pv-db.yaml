apiVersion: v1
kind: PersistentVolume
metadata:
  name: beeaxcrm-db-pv
spec:
  storageClassName: default
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
