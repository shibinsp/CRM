apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beeaxcrm-server-pvc
  namespace: beeaxcrm
spec:
  storageClassName: default
  volumeName: beeaxcrm-server-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
