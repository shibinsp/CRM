{{- if .Values.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "beeax.fullname" . }}-server
  namespace: {{ include "beeax.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "beeax.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.server.service.type | default "ClusterIP" }}
  selector:
    app.kubernetes.io/name: {{ include "beeax.name" . }}
    app.kubernetes.io/component: server
  ports:
    - name: http-tcp
      port: {{ .Values.server.service.port }}
      targetPort: {{ include "beeax.server.containerPort" . }}
      protocol: TCP
{{- end }}
