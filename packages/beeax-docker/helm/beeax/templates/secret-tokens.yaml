{{- if .Values.secrets.tokens.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.tokens.name | default "tokens" }}
  namespace: {{ include "beeax.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "beeax.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: tokens
stringData:
  accessToken: {{ include "beeax.secret.tokens.access" . | quote }}
{{- end }}
